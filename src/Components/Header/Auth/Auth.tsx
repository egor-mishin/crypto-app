import React, {FC} from "react"
import {IAuthProps} from "./Auth.props";

const Auth: FC<IAuthProps> = (): JSX.Element => {
    return (
        <>
            Auth
        </>
    )
}

export default Auth

