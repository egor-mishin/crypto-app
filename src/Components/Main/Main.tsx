import React, {FC} from "react"
import {IMainProps} from "./Main.props";

const Main: FC<IMainProps> = (): JSX.Element => {
    return (
        <>
            Main
        </>
    )
}

export default Main

