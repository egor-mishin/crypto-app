import React, {FC} from "react";
import {IFooterProps} from "../Footer/Footer.props";

const NavBar:FC<IFooterProps> = () => {
    return (
        <aside>
            <nav>NavBar</nav>
        </aside>
    )
}

export default NavBar

